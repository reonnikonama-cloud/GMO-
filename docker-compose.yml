version: '3.8'
services:
  trader-bot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "10000:10000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
